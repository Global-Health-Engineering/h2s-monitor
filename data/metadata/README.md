# Metadata

Codebook and data dictionary describing the variables, units, and encoding used in the datasets under `data/raw_data/` and `data/derived_data/`.

## Files

- `codebook.csv` — full variable descriptions, units, data types, and valid ranges for all columns appearing in the measurement CSV files
