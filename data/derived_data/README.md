# Derived Data

Processed or transformed data produced by the analysis scripts in `analysis/`. These files are generated from the raw data and should not be edited manually.

All derived data can be reproduced by running the corresponding script in `analysis/` against the source files in `raw_data/`.
